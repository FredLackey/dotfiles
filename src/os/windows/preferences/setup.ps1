$ErrorActionPreference = "Stop"

Write-Host "Applying Windows preferences..."

# Placeholder — add preference scripts here as they are developed.
# Each script should follow the idempotent pattern: check current value,
# only write if different, and track whether a restart/reload is needed.

Write-Host "Windows preferences applied."
