# Dotfiles Installation Test Results

## Test Metadata
- **Timestamp**: 20260404193426
- **Environment**: ubuntu-desktop
- **Docker Image**: dotfiles-test:ubuntu-desktop (built from testing/docker/ubuntu-desktop.Dockerfile, base: ubuntu:22.04)
- **Git Commit**: 132311c (test(ubuntu-wsl): document first passing Docker test run)
- **Overall Result**: PASS

## Execution Summary
- **Start Time**: 2026-04-04T19:34:26Z
- **End Time**: 2026-04-04T19:34:27Z
- **Duration**: 1s

## Container Configuration
- **Base Image**: ubuntu:22.04
- **Test User**: testuser (non-root, passwordless sudo)
- **Key Environment Variable**: `XDG_CURRENT_DESKTOP=GNOME` (signals desktop OS detection path)
- **Mode**: Remote install from GitHub (branch: main)
- **Pre-installed packages**: curl, unzip, sudo, ca-certificates

## Installation Phases

| Phase | Status | Duration | Notes |
|-------|--------|----------|-------|
| setup.sh download (curl) | PASS | <1s | Downloaded from GitHub raw URL |
| OS Detection | PASS | <1s | Linux + XDG_CURRENT_DESKTOP=GNOME → ubuntu-desktop path |
| Repository Download | PASS | <1s | Git not available; fell back to tarball download via curl |
| Ubuntu Desktop setup.sh | PASS | <1s | Script ran and exited 0 |

## OS Detection Flow

The main `src/setup.sh` routing logic worked correctly:

1. `uname -s` returned `Linux`
2. `/proc/version` did NOT contain "Microsoft" or "WSL" (no WSL markers)
3. `$WSL_DISTRO_NAME` was not set
4. `$XDG_CURRENT_DESKTOP` was set to `GNOME` (injected by Dockerfile)
5. Correctly routed to `src/os/ubuntu-desktop/setup.sh`

## Repository Download Method

Git was not available in the container (only curl, unzip, sudo, ca-certificates are pre-installed). The `src/setup.sh` correctly detected this and fell back to the tarball download path:

```
Git not available. Downloading dotfiles tarball...
```

The tarball was fetched from `https://github.com/FredLackey/dotfiles/tarball/main`, extracted to `~/.dotfiles`, and the ubuntu-desktop setup script was located and executed successfully.

## Ubuntu Desktop Setup Script State

The `src/os/ubuntu-desktop/setup.sh` is currently a stub:

```bash
#!/bin/bash
echo "Running Ubuntu Desktop setup..."
```

This is intentional. Per the test brief, ubuntu-desktop did not receive any new installers in the recent development cycle. The script correctly exits 0.

## Errors

None.

## Warnings

### Warning 1: Ubuntu Desktop Setup is a Stub

- **Phase**: Ubuntu Desktop setup.sh execution
- **Severity**: Informational (not a test failure)
- **Detail**: The `src/os/ubuntu-desktop/setup.sh` contains only a single echo statement. No tools are installed, no preferences are applied, no dotfiles are deployed. This means the test validates routing and download mechanics, but not any actual installation logic.
- **Impact**: Zero — by design for this test run. The goal was to verify the environment is not broken by shared changes, not to validate installer completeness.

### Warning 2: Git Not Available in Test Container

- **Phase**: Repository Download
- **Severity**: Informational
- **Detail**: The ubuntu-desktop Dockerfile does not install git. The tarball fallback path in `src/setup.sh` was exercised. This differs from a real Ubuntu desktop machine, which typically ships with git available or where apt can install it quickly. The tarball path works correctly, so this is not a problem.
- **Suggested improvement**: Consider adding git to the Dockerfile's pre-installed packages to more accurately simulate a real desktop machine and exercise the `git clone` path.

## Full Output Log

<details>
<summary>Click to expand full output</summary>

```
Test started at: 2026-04-04T19:34:26Z
Downloading setup.sh from GitHub (branch: main)...
URL: https://raw.githubusercontent.com/FredLackey/dotfiles/main/src/setup.sh

Git not available. Downloading dotfiles tarball...
Running Ubuntu Desktop setup...

========================================
TEST PASSED
========================================

Test ended at: 2026-04-04T19:34:27Z
Duration: 1s
Exit code: 0
```

</details>

## Recommendations

1. **Expand ubuntu-desktop/setup.sh**: The desktop setup is a stub. If ubuntu-desktop support is planned, begin adding installers following the same pattern used in ubuntu-server and ubuntu-wsl. The CLAUDE.md for src/os/ defines the required four-subdirectory structure (files/, functions/, installers/, preferences/).

2. **Add git to Dockerfile**: The ubuntu-desktop Dockerfile only installs curl, unzip, sudo, and ca-certificates. A real Ubuntu desktop environment has git available. Adding it to the Dockerfile would test the `git clone` path rather than the tarball fallback, giving more realistic coverage.

3. **No blocking issues found**: All shared changes (to macOS, ubuntu-server, ubuntu-wsl) had zero impact on ubuntu-desktop. The routing in `src/setup.sh` remains intact. This environment is confirmed non-broken.
